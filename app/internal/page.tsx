import { Shell } from "@/components/shell";

export default function InternalPage() {
  return (
    <Shell title="Internal Workspace Shell">
      <p>Visible to internal organization members only.</p>
    </Shell>
  );
}
