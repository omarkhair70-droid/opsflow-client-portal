import { Shell } from "@/components/shell";

export default function PortalPage() {
  return (
    <Shell title="Client Portal Shell">
      <p>Visible to client members only.</p>
    </Shell>
  );
}
